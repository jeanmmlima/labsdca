<h2>Editar Turmas</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/turmas/edit" method="POST">
        <input type="hidden" name="id" value="{{turma._id}}">
            <label for="nome">Nome</label>
                <input type="text" id="descricao" name="descricao" placeholder="Nome da Turma" class="form-control" value="{{turma.descricao}}">
            <label for="slug">Subturma:</label>
                <input type="text" id="subturma" name="subturma" placeholder="Subturma" class="form-control" value="{{turma.subturma}}">
                <br>
            <button type="submit" class="btn btn-dark">Editar Turma</button>
        </form>
    </div>
</div>