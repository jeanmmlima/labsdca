<h2>Editar Bancadas</h2>
<div class="card">
    <div class="card-body">
        <form action="/admin/bancadas/edit" method="POST" class="needs-validation" novalidate>
        <input type="hidden" name="id" value="{{bancada._id}}">
            <label for="nome">Descrição</label>
                <input type="text" id="descricao" name="descricao" placeholder="Bancada" class="form-control" value="{{bancada.descricao}}" required>
                <br>
            <button type="submit" class="btn btn-dark">Editar Bancada</button>
        </form>
    </div>
</div>