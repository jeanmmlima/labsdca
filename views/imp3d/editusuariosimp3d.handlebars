<div class="card">
    <div class="card-body">
        <h4>Nova Usuário Impressora 3D</h4>
        <form action="/imp3d/usuariosimp3d/edit" method="POST" class="needs-validation" novalidate>
            <input type="hidden" name="id" value="{{usuarioimp3d._id}}">
            <label for="usuario">Usuario: </label>
             <select name="usuario" class="custom-select" required>
                {{#each usuarios}}
                <option value="{{_id}}" {{selected _id ../usuarioimp3d.usuario}}>{{nome}} - {{email}}</option>
                {{else}}
                <option value="0">Nenhum Usuário Registrado</option>
                {{/each}}
            </select>
             <label for="comentario">Comentário: </label>
             <input type="text" id="comentario" name="comentario" placeholder="Comentário" class="form-control">            
             <br>

            <button class="btn btn-dark mt-4" type="submit">Editar Usuário Impressora 3D</button>
        </form>
    </div>
</div>